# AVG Release Gate Evidence — PRE-16 / PRE-15 / PRE-40

Generated by Hermes Engineer (CTO) on 2026-07-14. All numbers below were produced by
actually running the commands against the local `Automated-Video-Generator` checkout.

## Branch situation (IMPORTANT)
- The subtitle burn-in (Feature A) + batch queue (Feature B) implementation is **complete and
  tested on branch `ci/pre14-foundation`** but was **never merged to `main`**.
- `main` HEAD = `6c65ba3` (docs only). Feature commits `677ca85`, `0297ec6`, `b22066f`,
  `3383a65`, `4feeb42` are on `ci/pre14-foundation` only.
- The spec doc exists at `docs/SPEC_SUBTITLE_BURNIN_BATCH_QUEUE.md` (NOTE: PRE-15/PRE-16 issue
  text references a wrong path `docs/spec-avg-subtitle-burnin-batch-queue.md` — typo).
- Consequence: PRE-61 and PRE-62 are marked `done` but their code is not on `main`. This is the
  true blocker for PRE-15/PRE-16 — not missing engineering, but a missing merge.

## Verification run (on `ci/pre14-foundation` @ 12fde64 after D1/D2 fixes)
| Gate | Command | Result |
|------|---------|--------|
| G1 Typecheck | `npm run typecheck` | **PASS** (exit 0) |
| Feature A unit | `npx tsx --test src/lib/captions.test.ts` | **23/23 PASS** |
| Feature B unit | `npx tsx --test src/adapters/cli/batch-queue.test.ts` | **8/8 PASS** |
| G2 Full unit | `npm run test:unit` | 106 pass / 1 fail / 1 skip |
| G3 Lint | `npm run lint` | not run (low RAM); see note |

### The 1 "fail" in G2
- Failing file: `src/application/pipeline-app.service.test.ts`.
- Run **in isolation** it passes 2/2 (verified). In the full suite it failed with a
  connection attempt to local TTS servers (Voicebox :17493, XTTS :8020, OpenAI-Local :8880)
  that are not running on this host — an environment/network dependency leak, NOT a logic
  defect in the subtitle/batch features.
- Classification: env-gated flake, same class as G5 render E2E (G6 Docker needs chromium which
  is present only inside the image). It must be made hermetic before GA, but it is not evidence
  that the AVG features are broken.

## Release blockers fixed this heartbeat (CTO-owned, D1/D2 from RELEASE_GATE_TEST_PLAN.md)
- **D1** `Dockerfile:36` HEALTHCHECK pointed at `/health` (404). Real route is `/api/health`
  (`src/adapters/http/api-routes.ts:45`). Fixed to `http://localhost:3001/api/health`.
- **D2** `src/constants/config.ts:16` `PORT = Number(process.env.PORT || 3001)` inherited the
  ambient `PORT=3100` (Paperclip host) → EADDRINUSE. Fixed: resolution order is now
  `AVG_PORT` > `.env` `PORT` > default `3001`; ambient `PORT` is ignored unless `AVG_PORT` is
  set. Containers/CI stay deterministic; pass `AVG_PORT` to override.
- Both fixes committed as `12fde64` on `ci/pre14-foundation`.

## Remaining action for PRE-16 (no-go until done)
1. Merge `ci/pre14-foundation` → `main` (clean-ish: only divergence is a duplicate "GitHub
   Sponsors badge" docs commit on each branch — conflict-safe). This makes PRE-61/PRE-62 real on
   main and unblocks the gate.
2. Make `pipeline-app.service.test.ts` hermetic (mock TTS / skip when servers absent) so G2 is
   deterministically green.
3. Run `npm run docker:build` + container `/api/health` probe to confirm G6 (chromium present in
   image).
4. Then flip PRE-16 → `done` (QA), PRE-40 → `done`, and tag the release.

## Note on memory
This Windows host runs ~6GB RAM with frequent <150MB free. Heavy Node suites are time-boxed and
the stray untracked WIP files `src/lib/free-music.ts`, `src/lib/ollama-bootstrap.ts` (left by
another agent) were relocated to the run scratch dir during verification and restored to `main`'s
working tree afterward — they are NOT part of the AVG feature and should be triaged separately.
